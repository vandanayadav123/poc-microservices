"use strict";

class AppRouter {
  constructor(app, router) {
    this.router = router;
  }
  loadRouters() {
    // this.router.get('/login',res.send('congrats'))
  }
  Infinity() {
    this.loadRouters();
    return this.router;
  }
}

module.exports = AppRouter;
